# workflowtest
Testing GitHub Actions


## Commands to start running project locally with Docker  
Boot up with this command:
```
docker compose up --build -d
```
Stop with this command:  
```
docker compose down
```